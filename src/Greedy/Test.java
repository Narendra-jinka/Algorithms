package Greedy;

import java.util.*;
public class Test {

	public static void main(String[] args) {
        /*List<ArrayList<Integer>> items = new ArrayList<>();

        // Adding items
        items.add(new ArrayList<>(Arrays.asList(25, 18)));
        items.add(new ArrayList<>(Arrays.asList(24, 15)));
        items.add(new ArrayList<>(Arrays.asList(15, 10)));

        // Calculate profit-to-weight ratio and sort in descending order
        Collections.sort(items, (item1, item2) -> {
            double ratio1 = (double) item1.get(0) / item1.get(1); // profit / weight
            double ratio2 = (double) item2.get(0) / item2.get(1);
            return Double.compare(ratio2, ratio1); // Sort in descending order
        });

        // Display the corrected order
        System.out.println("Order after sorting based on correct profit-to-weight ratio:");
        for (ArrayList<Integer> item : items) {
            System.out.println("Profit: " + item.get(0) + ", Weight: " + item.get(1));
        }*/
		
		char[] order=new char[7];
		for(char c:order) {
			System.out.print(c+" ");
		}
		
    }

}
